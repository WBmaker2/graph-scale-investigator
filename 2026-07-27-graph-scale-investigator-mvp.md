# 그래프 눈금 수사대 MVP 구현 계획

## 1. 문서 상태

| 항목 | 내용 |
|---|---|
| 상태 | 구현 전 계획 승인 대기 |
| 작성일 | 2026-07-27 |
| 구현 여부 | 구현하지 않음 |
| 배포 여부 | 배포하지 않음 |
| 대상 | 초등 5~6학년 수학 |
| 핵심 주제 | 축, 눈금 간격, 시작점, 단위가 자료 해석에 미치는 영향 |
| 기존 앱과의 차별점 | 자료를 수집하거나 그래프 종류를 연습하는 앱이 아니라, 같은 자료의 표현이 판단을 어떻게 바꾸는지 검증하고 고치는 앱 |

이 문서는 `그래프 눈금 수사대`의 교육 목표, 화면 흐름, 고정 콘텐츠, 판정 규칙, 접근성, 검증 계획을 정의합니다. 이 문서가 승인되기 전에는 코드, 저장소, 이미지, 패키지, 테스트, 커밋, 배포를 만들지 않습니다.

## 2. 배경과 문제 정의

`우리 반 통계청`은 학급 투표를 모아 막대그래프·원그래프·띠그래프로 비교하는 자료 수집과 시각화 앱입니다. `그래프 눈금 수사대`는 같은 자료를 서로 다른 축과 눈금으로 표현한 뒤, 화면이 주는 인상이 실제 수치 차이와 일치하는지 살피는 미디어·수학 문해 활동으로 분리합니다.

학생이 해결할 문제는 다음과 같습니다.

1. 그래프의 막대 높이나 선의 기울기만 보고 결론을 내리지 않고 실제 숫자와 눈금 간격을 확인한다.
2. 축이 0에서 시작하지 않거나 눈금 간격이 불규칙할 때 어떤 인상이 커지거나 작아지는지 설명한다.
3. `한 칸 = 몇 개`인지, 단위가 무엇인지, 표시된 자료의 범위가 어디까지인지 확인한다.
4. 그래프가 거짓인지 여부만 판정하지 않고, 자료를 정확하고 읽기 쉽게 표현하도록 고친다.
5. 같은 자료라도 목적에 따라 강조 그래프와 전체 변화 그래프가 다를 수 있음을 구분한다.

## 3. 교육 목표

### 3.1 학습 목표

학생은 활동을 마친 뒤 다음을 할 수 있어야 합니다.

- 그래프의 제목, 가로축, 세로축, 단위, 눈금 간격을 찾아 말할 수 있습니다.
- 막대 높이 또는 선의 기울기와 실제 수치 차이를 분리해 확인할 수 있습니다.
- 시작점, 눈금 간격, 단위, 축 범위가 바뀌면 시각적 인상이 달라질 수 있음을 설명할 수 있습니다.
- 같은 자료의 두 그래프를 비교하고 `자료의 차이`와 `표현의 차이`를 구분할 수 있습니다.
- 목적에 맞는 그래프를 선택하고, 선택한 축 설정의 이유를 한 문장으로 쓸 수 있습니다.
- 문제 있는 그래프를 고칠 때 무엇을 바꾸었는지와 왜 바꾸었는지를 근거와 함께 설명할 수 있습니다.

### 3.2 성취기준 연결 방향

정확한 성취기준 번호는 구현 전 사용하는 학교 교육과정 자료를 다시 대조합니다. 현재 계획에서는 5~6학년 수학의 자료 수집·정리·해석, 표와 그래프의 관계, 자료에 근거한 판단 활동에 연결합니다.

앱이 교육과정 전체를 대신하는 것이 아니라, 그래프를 읽고 표현을 비판적으로 점검하는 한 차시 활동을 지원한다는 범위를 지킵니다.

### 3.3 평가 관점

점수는 그래프의 모양을 맞혔는지가 아니라 다음 근거를 얼마나 사용했는지 평가합니다.

| 평가 요소 | 관찰 가능한 행동 |
|---|---|
| 요소 확인 | 제목·축·단위·눈금 간격을 찾아 표시 |
| 수치 확인 | 그래프를 보기 전에 실제 값 표를 확인 |
| 표현 판단 | 시각적 인상과 실제 차이가 어긋나는 까닭 설명 |
| 수정 이유 | 축 시작점·간격·범위·단위를 바꾼 이유 작성 |
| 전이 | 새로운 자료에서도 같은 점검 절차 적용 |

## 4. 기존 앱과의 중복 방지

| 기존 앱 | 기존 핵심 경험 | 그래프 눈금 수사대에서 제외할 것 |
|---|---|---|
| 우리 반 통계청 | 실제 학급 투표 수집, 실시간 방, 그래프 종류 비교 | 학생 투표방, 실시간 집계, 학급 결과 공유 |
| 가능성 예보국 | 반복 실험 자료의 가능성 예측과 수정 | 확률 예보, 시행 횟수 누적, 선택 결과 예측 |
| AI 데이터 품질검사소 | 구조화된 데이터의 누락·중복·대표성 점검 | 데이터 행 정비, AI 성능 전후 비교 |
| 팩트체크 편집국 | 자료 출처와 주장 사실성 판정 | 외부 출처 조사, 참·거짓 판정 |
| 식물 관찰 일기 | 실제 관찰 기록과 성장 그래프 | 장기 기록 입력, 생물 성장 일지 |
| 철새 관찰 일지 | 관찰 자료 수집과 막대그래프 작성 | 동물 관찰 카드 수집 |

차별화의 핵심은 `새 자료를 그래프로 만드는 일`이 아니라 `이미 주어진 동일 자료가 표현 방식 때문에 다르게 보일 때 그 차이를 수사하는 일`입니다.

## 5. 과학·수학적 개념 계약

### 5.1 그래프를 읽는 고정 절차

모든 미션에서 다음 순서를 반복적으로 노출합니다.

1. 제목을 읽습니다.
2. 가로축과 세로축의 대상과 단위를 확인합니다.
3. 눈금 한 칸의 값을 계산합니다.
4. 실제 값 표와 그래프 값을 대조합니다.
5. 그래프의 인상과 실제 수치 차이를 비교합니다.
6. 목적에 맞는 표현인지 판단하고 필요하면 수정합니다.

### 5.2 `왜곡`의 교육적 정의

앱에서 왜곡은 그래프가 반드시 거짓이라는 뜻이 아니라, 표현이 실제 차이보다 훨씬 크거나 작게 느껴지도록 만들 수 있다는 뜻으로 사용합니다. 축을 0에서 시작하지 않는 것이 모든 그래프에서 항상 잘못된 것은 아니므로, 맥락과 그래프 종류를 함께 판단하게 합니다.

### 5.3 막대그래프와 선그래프의 차이

- 막대그래프는 범주별 양을 비교하므로, MVP의 막대 미션에서는 기본적으로 0에서 시작하는 축을 권장합니다.
- 선그래프는 시간에 따른 변화와 추세를 볼 때 쓰므로, 변화의 작은 차이를 확대해 보는 보조 그래프를 허용하되 전체 범위 그래프와 함께 제시합니다.
- 학생에게 `0이 아니면 무조건 틀림`이라는 규칙을 주지 않습니다.

### 5.4 단위와 눈금 간격

- 세로축에 `명`, `개`, `cm`, `원` 등 단위를 표시합니다.
- 눈금 사이의 중간 표시가 있으면 한 칸의 값을 다시 계산해야 합니다.
- 0, 2, 4, 6처럼 일정한 간격과 0, 5, 10, 20처럼 불규칙한 표시를 구분합니다.
- MVP에서는 학생이 불규칙한 눈금을 직접 읽어야 하는 사례를 제한적으로 사용하고, 수사 힌트를 제공합니다.

## 6. 제품 계약

### 6.1 입력과 출력

| 구분 | MVP 계약 |
|---|---|
| 입력 | 앱에 포함된 고정 자료 세트, 학생의 선택·표시·간단한 설명 |
| 서버 | 없음. 모든 미션은 브라우저에서 결정적으로 실행 |
| 저장 | 기본적으로 새로고침 시 초기화. 선택적으로 결과 카드 이미지·텍스트 복사만 제공 |
| 채점 | 정답 그래프 하나만 강요하지 않고 필수 근거 요소를 조합해 판정 |
| 언어 | 초등학생용 쉬운 한국어 |
| 대상 기기 | 전자칠판, 태블릿, 노트북, 작은 화면 휴대전화 |

### 6.2 핵심 상태

```ts
type AxisConfig = {
  min: number;
  max: number;
  tickStep: number;
  unit: string;
  label: string;
};

type GraphCase = {
  id: string;
  title: string;
  chartType: 'bar' | 'line';
  categories: string[];
  values: number[];
  unit: string;
  intendedPurpose: 'compare' | 'trend' | 'overview';
  misleadingVariant?: AxisConfig;
  fairVariants: AxisConfig[];
  evidencePrompts: EvidencePrompt[];
};

type EvidencePrompt = {
  id: string;
  label: string;
  correctOptions: string[];
  explanation: string;
};

type InvestigationState = {
  missionId: string;
  phase: 'observe' | 'check-values' | 'explain' | 'repair' | 'reflect';
  selectedGraphId?: string;
  selectedEvidence: string[];
  repairedAxis?: AxisConfig;
  note: string;
  completed: boolean;
};
```

실제 구현 시 타입 정의는 기능별 파일로 분리하고, 단일 코드 파일이 500줄을 넘지 않도록 합니다.

## 7. 학습 흐름

### 7.1 전체 흐름

```text
수사 안내
  → 같은 자료의 두 그래프 관찰
  → 축·단위·눈금 확인
  → 실제 값 표와 대조
  → 인상과 수치 차이 설명
  → 공정한 그래프 축 고치기
  → 목적에 맞는 표현 선택
  → 수사 기록 카드 완성
```

### 7.2 수사 도구

- `값 보기`: 그래프 옆에 원자료 표를 열고 닫습니다.
- `눈금 한 칸 계산`: 인접 눈금 두 개를 선택해 차이를 확인합니다.
- `축 0으로 보기`: 축을 0부터 보았을 때 막대 인상이 어떻게 달라지는지 비교합니다.
- `실제 차이 표시`: 두 범주의 수치 차이를 숫자로 표시합니다.
- `확대/전체 보기`: 선그래프의 작은 변화를 확대해 보되 전체 범위도 함께 제공합니다.
- `근거 형광펜`: 제목, 단위, 눈금, 값 표 중 근거가 된 요소를 표시합니다.

도구는 자동 정답 표시가 아니라 확인 절차를 돕는 힌트로 사용합니다.

## 8. MVP 미션 구성

MVP는 튜토리얼 1개와 5개 미션으로 구성합니다. 한 미션은 3~5분, 전체는 20분 안팎을 목표로 합니다.

### 미션 0. 수사관 훈련소

- 동일한 네 값으로 만든 정직한 막대그래프와 과장된 막대그래프를 나란히 보여 줍니다.
- 학생이 제목, 세로축, 단위, 눈금 한 칸을 순서대로 클릭합니다.
- `눈에 보이는 차이`와 `실제 숫자 차이`를 두 문장으로 구분합니다.
- 그래프를 보기 전에 값 표를 확인하는 절차를 안내합니다.

### 미션 1. 잘린 축 사건

- 자료: 네 반의 재활용 병 수 48, 50, 52, 54개.
- 그래프 A는 0부터, 그래프 B는 46부터 시작합니다.
- 질문: 어떤 그래프가 차이를 더 크게 느끼게 하는가, 실제 최대·최소 차이는 얼마인가.
- 수정: 막대그래프의 축을 0부터 시작하도록 고칩니다.
- 핵심 개념: 시작점과 실제 차이의 분리.

### 미션 2. 눈금 한 칸 실종 사건

- 자료: 일주일 동안 모은 우유팩 0, 10, 20, 30개.
- 세로축이 0, 5, 10, 15로 표시되지만 막대 눈금은 두 칸마다 숫자가 있는 변형을 사용합니다.
- 질문: 숫자가 없는 칸의 값은 얼마인지, 그래프가 표현하는 단위는 무엇인지 확인합니다.
- 수정: 보조 눈금과 숫자 표시를 일관되게 정리합니다.
- 핵심 개념: 눈금 간격과 단위 해석.

### 미션 3. 단위 바꾸기 사건

- 자료: 운동장 길이 120m, 150m, 180m.
- 그래프 A는 m, 그래프 B는 cm로 표시하고 축 제목을 일부 가린 변형을 제공합니다.
- 질문: 단위가 달라져도 실제 비교 관계는 같은지 확인합니다.
- 수정: 단위와 축 라벨을 복원하고, 숫자만 커졌다고 양이 더 커진 것은 아님을 설명합니다.
- 핵심 개념: 수의 크기와 측정 단위의 관계.

### 미션 4. 선의 기울기 착시 사건

- 자료: 월별 도서 대출 40, 42, 44, 46권.
- 좁은 세로축 선그래프와 전체 범위 선그래프를 비교합니다.
- 질문: 어느 그래프가 변화가 큰 것처럼 보이는지, 실제로 한 달마다 몇 권씩 늘었는지 말합니다.
- 수정: 시간 변화 목적에 맞는 확대 그래프와 전체 범위 그래프를 함께 제시합니다.
- 핵심 개념: 시각적 기울기와 실제 증가량의 분리.

### 미션 5. 목적에 맞는 그래프 사건

- 자료: 네 가지 급식 메뉴 선호도 12, 15, 16, 17명.
- 학생은 `범주별 양 비교`, `시간에 따른 변화`, `전체 비율 보기` 중 목적을 선택합니다.
- 목적에 따라 막대그래프, 선그래프, 비율 표현 중 적절한 방식을 고릅니다.
- 정답은 그래프 종류만으로 결정하지 않고 제목·축·단위·자료의 성격을 함께 평가합니다.
- 핵심 개념: 공정한 그래프는 모양 하나가 아니라 목적과 읽기 가능성을 고려한 표현이라는 점.

## 9. 판정과 피드백 규칙

### 9.1 근거 기반 판정

학생 답변은 다음처럼 부분적으로 인정합니다.

| 답변 상태 | 피드백 |
|---|---|
| 값과 축을 모두 확인 | `수치와 표현을 함께 확인했어요.` |
| 인상만 설명 | `그래프의 모양뿐 아니라 실제 값도 찾아보세요.` |
| 시작점만 지적 | `좋아요. 눈금 간격과 단위도 확인해 볼까요?` |
| 0 시작을 모든 상황에 강요 | `막대그래프에서는 유용하지만, 선그래프는 목적과 전체 범위도 함께 살펴야 해요.` |
| 단위만 바뀐 것을 크기 변화로 판단 | `단위가 바뀌면 숫자는 달라져도 실제 양은 같을 수 있어요.` |

### 9.2 설명 문장 조립

학생은 다음 문장 틀 중 하나를 선택해 완성합니다.

- `실제 값의 차이는 ___인데, 그래프에서는 ___처럼 보입니다.`
- `이 그래프의 한 칸은 ___ ___이므로, ___의 값은 ___입니다.`
- `나는 ___을(를) 고쳤습니다. 자료를 ___하게 비교하기 위해서입니다.`
- `이 그래프는 ___을 비교하는 목적에 더 알맞습니다. 근거는 ___입니다.`

자유 입력은 선택 사항으로 두고, 저학년이 아닌 5~6학년 대상이므로 짧은 직접 입력을 점진적으로 허용합니다.

## 10. 콘텐츠 데이터 설계

### 10.1 고정 자료 원칙

- 실제 학생 개인정보와 학급 자료를 사용하지 않습니다.
- 값은 정수 중심으로 구성하고, 눈금 계산이 가능한 범위로 제한합니다.
- 각 미션은 실제 값 표를 기준 데이터로 삼고 모든 그래프가 같은 값을 참조합니다.
- 그래프 변형은 값 배열을 바꾸지 않고 축 설정만 바꾸는 방식으로 만듭니다.
- 교육적 착시와 단순한 그래프 오류를 구분해 콘텐츠 검수표에 기록합니다.

### 10.2 데이터 검수표

| 검수 항목 | 확인 내용 |
|---|---|
| 합계·최대·최소 | 그래프와 원자료의 값이 일치하는가 |
| 단위 | 제목·축·설명에서 같은 단위를 사용하는가 |
| 눈금 | 모든 표시 간격이 의도한 값과 일치하는가 |
| 시작점 | 막대그래프에서 0 시작 여부와 교육적 이유가 명확한가 |
| 목적 | 그래프 종류가 자료의 성격과 맞는가 |
| 문장 | `거짓`보다 `과장되어 보일 수 있음`처럼 정확한 표현을 쓰는가 |
| 접근성 | 색 없이도 값과 차이를 확인할 수 있는가 |

## 11. 화면 설계

### 11.1 화면 목록

| 화면 | 주요 요소 | 학습 역할 |
|---|---|---|
| 시작 화면 | 제목, 수사 안내, 학년·수업 시간 안내, 업데이트 내역 버튼 | 활동 맥락 제공 |
| 훈련 화면 | 그래프, 값 표, 체크 항목 | 읽기 절차 연습 |
| 미션 화면 | 사건 카드, 비교 그래프, 수사 도구 | 근거 수집 |
| 축 수리 화면 | 시작점·최댓값·눈금 간격·단위 입력 | 그래프 수정 |
| 설명 화면 | 문장 틀, 근거 선택, 짧은 입력 | 수학적 설명 |
| 결과 카드 | 확인한 단서, 고친 항목, 최종 문장 | 학습 회고와 공유 |

### 11.2 핵심 레이아웃

- 왼쪽 또는 위쪽: 사건 설명과 원자료 표.
- 중앙: 그래프 비교 영역.
- 오른쪽 또는 아래쪽: 수사 도구와 근거 문장.
- 모바일에서는 `자료 → 그래프 → 근거 → 수정` 순서의 세로 흐름으로 전환합니다.
- 그래프만 지나치게 크게 만들지 않고 값 표와 축 설명을 같은 화면에서 접근 가능하게 합니다.

### 11.3 업데이트 내역

향후 구현 시 시작 화면 또는 결과 카드 상단에 작은 `업데이트 내역` 버튼을 둡니다. 다음 내용을 기록합니다.

- 개발일과 최초 MVP 공개일
- 그래프 판정 문구를 수정한 날짜와 내용
- 새로운 사건 자료를 추가한 날짜
- 접근성·모바일 표시 개선 내역

## 12. 접근성 및 초등학생 UX

- 색상만으로 정직한 그래프와 주의 그래프를 구분하지 않습니다.
- 막대·선·축에 텍스트 라벨과 수치 표를 함께 제공합니다.
- 그래프 요소의 최소 터치 영역은 44px 이상으로 계획합니다.
- 키보드만으로 그래프 선택, 근거 표시, 축 수정이 가능해야 합니다.
- 포커스 표시와 현재 단계 표시를 명확히 합니다.
- `왜 틀렸나요?` 대신 `어떤 단서를 더 확인하면 좋을까요?`처럼 오류 낙인을 줄이는 문구를 사용합니다.
- 긴 설명은 `한 줄 힌트 → 자세히 보기`로 나눕니다.
- 애니메이션은 축 변경 결과를 보여 주는 짧은 전환으로 제한하고, 모션 줄이기 설정을 존중합니다.
- 모바일 화면에서 표가 가로로 잘리지 않도록 카드형 표 또는 가로 스크롤을 제공합니다.

## 13. 교사 수업 운영

### 13.1 15분 수업 흐름

1. 교사가 두 그래프를 보여 주고 `어느 쪽 차이가 더 커 보이나요?`를 묻습니다.
2. 학생이 실제 값 표를 확인하고 최대·최소 차이를 계산합니다.
3. 개인 또는 2인 모둠이 축·눈금·단위 단서를 표시합니다.
4. 학생이 공정한 그래프를 수정하고 이유를 문장으로 완성합니다.
5. 교사는 `그래프가 거짓인가, 표현이 과장되어 보이는가`를 토론하게 합니다.

### 13.2 종이·실물 연계

- 같은 자료를 모눈종이에 0 시작 그래프와 확대 그래프로 직접 그립니다.
- 자를 이용해 막대 높이와 축 간격을 비교합니다.
- 신문·광고의 그래프를 가져와 앱의 수사 절차로 점검하되, 개인 식별 정보는 사용하지 않습니다.

## 14. 구현 예정 구조

```text
src/
  data/
    graphCases.ts
    feedbackRules.ts
  features/graph-investigator/
    GraphInvestigatorApp.tsx
    MissionScreen.tsx
    GraphComparePanel.tsx
    AxisRepairPanel.tsx
    EvidencePanel.tsx
    ResultCard.tsx
    useInvestigationState.ts
  lib/
    graphScale.ts
    graphValidation.ts
  styles/
    graph-investigator.css
```

구조는 실제 저장소의 기존 프레임워크를 확인한 뒤 조정합니다. 차트 라이브러리를 도입하더라도 데이터 계산과 판정 규칙은 별도 순수 함수로 두어 테스트 가능하게 합니다.

## 15. 핵심 로직 계획

### 15.1 눈금 계산

```ts
function getTickValue(min: number, tickStep: number, index: number): number {
  return min + tickStep * index;
}
```

구현 시 다음을 검증합니다.

- `tickStep`이 0보다 큰가.
- 최대값이 최소값보다 큰가.
- 그래프 높이 계산에 실제 값 배열을 사용하고 축 설정을 별도로 적용하는가.
- 단위 변환은 값의 의미를 바꾸지 않고 표시값과 라벨을 함께 바꾸는가.

### 15.2 시각적 인상과 실제 차이

앱은 학생에게 시각적 인상을 자동으로 `정답·오답`으로 단정하지 않고 다음 정보를 계산해 보여 줍니다.

- 실제 최대·최소 차이
- 축 범위 대비 차이 비율
- 0 시작 여부
- 눈금 간격의 일정성
- 단위와 값 표의 일치 여부

`과장 정도`는 자연 법칙처럼 제시하지 않고, 해당 그래프 설정에서 막대 높이가 차이를 얼마나 크게 보이게 하는지 설명하는 보조 지표로만 사용합니다.

## 16. 검증 계획

구현 단계에서 다음 순서로 검증합니다. 현재는 실행하지 않습니다.

### 16.1 단위 검증

- 눈금 계산과 축 범위 계산
- 0 시작·비0 시작 그래프의 값 위치
- 단위 변환 표시
- 값 배열과 그래프 라벨 일치
- 불규칙 눈금 입력 거부 또는 안내

### 16.2 콘텐츠 검증

- 모든 미션의 표 값과 그래프 값 대조
- 정답 근거와 피드백 문구 대조
- 막대그래프와 선그래프 목적 설명의 정확성
- `0이 아니면 무조건 틀림` 오개념 방지 문구 확인

### 16.3 UI 스모크 검증

- 시작 → 미션 → 축 수정 → 설명 → 결과 카드 흐름
- 새로고침 후 서버 의존 없이 다시 시작
- 작은 화면에서 그래프와 표가 모두 읽히는지 확인
- 키보드 포커스 순서와 버튼 이름 확인
- 업데이트 내역 버튼이 열리고 닫히는지 확인

### 16.4 학생 관점 검토

- 그래프만 보고 다음 버튼을 누를 수 없고 값 표 확인이 자연스럽게 유도되는가
- `한 칸`과 `단위`의 의미를 혼동하지 않는가
- 설명이 단순히 `축을 0으로 고치기` 암기로 끝나지 않는가
- 여러 답이 가능한 목적 선택 미션에서 근거에 따라 인정되는가

## 17. 완료 조건

구현 완료로 판단하려면 다음을 모두 만족해야 합니다.

- 튜토리얼과 5개 미션이 서버 없이 실행됩니다.
- 모든 그래프는 동일한 원자료를 사용하고 축 설정만으로 변형됩니다.
- 학생이 제목·축·단위·눈금·실제 값을 확인하는 흐름을 거칩니다.
- 잘린 축, 눈금 간격, 단위, 확대 선그래프 사례를 각각 경험합니다.
- 막대그래프의 0 시작을 유용한 기본값으로 다루되 모든 그래프에 기계적으로 강요하지 않습니다.
- 결과 카드에 학생이 고친 항목과 근거 문장이 남습니다.
- 색상에 의존하지 않는 수치·라벨·패턴 표현이 제공됩니다.
- `업데이트 내역` 버튼과 기록이 포함됩니다.
- 단일 소스 파일이 500줄을 넘지 않습니다.
- 계획의 중복 방지 기준이 기존 카탈로그 앱과의 비교 리뷰에서 유지됩니다.

## 18. 위험과 대응

| 위험 | 대응 |
|---|---|
| 학생이 `0이 아니면 항상 거짓`으로 암기 | 막대·선그래프 목적을 분리하고 확대 그래프와 전체 범위를 함께 제시 |
| 그래프 모양만 보고 수치 확인을 건너뜀 | 값 표 확인 단계를 필수화하고 근거 표시 후 수정 가능하게 함 |
| 불규칙 눈금이 계산 부담을 키움 | MVP에서는 사례를 제한하고 한 칸 계산 힌트를 제공 |
| 그래프 라이브러리가 축을 자동으로 조정함 | 축 계산을 앱 데이터 모델에서 명시하고 자동 스케일 기본값을 끔 |
| `왜곡`이라는 말이 그래프를 거짓으로 오해하게 함 | `과장되어 보일 수 있음`, `표현을 점검할 필요가 있음`을 병행 사용 |
| 기존 통계 앱과 화면이 비슷해짐 | 실시간 입력·투표·그래프 종류 선택을 제외하고 수사 절차와 축 수리를 중심에 둠 |
| 작은 화면에서 표와 그래프가 동시에 보이지 않음 | 단계형 세로 레이아웃과 값 표 고정 버튼을 제공 |

## 19. 구현 순서

1. 기존 저장소의 프레임워크·차트 사용 여부·스타일 규칙을 확인합니다.
2. 미션 데이터와 피드백 규칙을 먼저 확정하고 콘텐츠 검수를 받습니다.
3. 축 계산·값 대조·판정 로직을 순수 함수로 작성합니다.
4. 그래프 비교 패널과 값 표를 구현합니다.
5. 축 수리 패널과 근거 문장 조립을 연결합니다.
6. 튜토리얼과 5개 미션을 등록합니다.
7. 결과 카드·업데이트 내역·접근성 요소를 추가합니다.
8. 단위·콘텐츠·UI 검증을 실행합니다.
9. 학생 관점으로 전체 흐름을 검토하고 문구를 조정합니다.
10. 사용자가 별도로 요청한 경우에만 커밋·푸시·배포를 진행합니다.

## 20. 승인 게이트

구현 시작 전 다음 사항을 확인받습니다.

- 그래프 눈금·축 시작점·단위·표현 인상이라는 학습 초점이 적절한가
- `우리 반 통계청`과의 차별화 기준이 충분한가
- 고정 미션 자료와 수치가 수업에 적합한가
- 막대그래프와 선그래프의 축 규칙 설명이 과도하게 단순화되지 않았는가
- `그래프 눈금 수사대`라는 이름과 수사관 콘셉트가 5~6학년에게 자연스러운가

사용자 승인 전에는 구현을 시작하지 않습니다.

